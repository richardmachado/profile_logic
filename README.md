login page and profile page working
if no profile, pushes you to /addprofile

fixes needed
login page needs error handling if invalid username/password, no such username
add profile page, needs to submit the user_id
register page